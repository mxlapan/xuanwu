# deployment secrets and runtime state
deploy/**/.env
deploy/panel/data/
deploy/node/certs/
deploy/node/logs/

# node runtime state — scaffolded by deploy.sh, never committed
deploy/node/data/

# per-host compose overrides (NPM network, ports, ...)
deploy/**/docker-compose.override.yml

# go
/panel
/agent

# local tooling / editor
.claude/

# backups copied out by ./deploy.sh backup
/panel-backup-*.db

*.log
*.zip
tmp/
